---
import '../styles/global.css';
export interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
}
const {
  title = 'DD에어컨 | 남양주 에어컨 전문 설치·판매·청소',
  description = '남양주·다산신도시 에어컨 전문 업체 DD에어컨. 천장형 4WAY·스탠드·벽걸이 설치, 에어컨 청소·수리·이전. 당일 출장 가능. 70건+ 시공 실적.',
  ogImage = '/images/bg/h1.jpg'
} = Astro.props;
const canonicalURL = Astro.site ? new URL(Astro.url.pathname, Astro.site) : null;
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---
<!DOCTYPE html>
<html lang='ko'>
<head>
  <meta charset='UTF-8' />
  <meta name='viewport' content='width=device-width, initial-scale=1.0' />
  <title>{title}</title>
  <meta name='description' content={description} />
  <meta name='keywords' content='남양주에어컨설치,다산신도시에어컨,에어컨설치업체,천장형에어컨,4WAY에어컨,에어컨청소,에어컨수리,DD에어컨,구리에어컨,하남에어컨' />
  <meta name='robots' content='index, follow' />
  <link rel='canonical' href={canonicalURL?.toString() ?? Astro.url.href} />
  <meta property='og:type' content='website' />
  <meta property='og:title' content={title} />
  <meta property='og:description' content={description} />
  <meta property='og:image' content={`${base}${ogImage}`} />
  <meta property='og:locale' content='ko_KR' />
  <meta property='og:site_name' content='DD에어컨' />
  <meta name='twitter:card' content='summary_large_image' />
  <meta name='twitter:title' content={title} />
  <meta name='twitter:description' content={description} />
  <meta name='twitter:image' content={`${base}${ogImage}`} />
  <meta name='site-base' content={base} />
  <script type='application/ld+json' set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": ["LocalBusiness","HVACBusiness"],
    "name": "DD에어컨",
    "description": "남양주·다산신도시 에어컨 전문 설치·판매·청소 업체",
    "telephone": "010-7237-2122",
    "url": "https://megaboy81-boop.github.io/ddinair-site/",
    "sameAs": ["https://blog.naver.com/ddinair"],
    "address": {
      "@type": "PostalAddress",
      "streetAddress": "진건읍 고재로 186 1층",
      "addressLocality": "남양주시",
      "addressRegion": "경기도",
      "addressCountry": "KR"
    },
    "geo": {
      "@type": "GeoCoordinates",
      "latitude": 37.6262,
      "longitude": 127.1889
    },
    "openingHours": "Mo-Su 08:00-20:00",
    "priceRange": "₩₩"
  })} />

  <link rel='preconnect' href='https://fonts.googleapis.com' />
  <link rel='preconnect' href='https://fonts.gstatic.com' crossorigin />
  <link href='https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Black+Han+Sans&family=Bebas+Neue&display=swap' rel='stylesheet' />
  <link rel='stylesheet' href='https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable.min.css' />
  <!-- CDN — GSAP + Lenis + SplitType -->
  <Fragment set:html={'<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"><\/script><script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/ScrollTrigger.min.js"><\/script><script src="https://cdn.jsdelivr.net/npm/@studio-freight/lenis@1.0.42/dist/lenis.min.js"><\/script><script src="https://unpkg.com/split-type@0.3.4/umd/index.min.js"><\/script>'} />

  <style is:global>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    :root {
      --black: #0A1628;
      --dark: #FFFFFF;
      --dark2: #F0F5FC;
      --blue: #4FC3F7;
      --blue-deep: #1E88E5;
      --blue-light: #81D4FA;
      --blue-glow: rgba(21,101,192,0.15);
      --cream: #F0F4F8;
      --text-dim: rgba(255,255,255,0.55);
      --text-muted: rgba(255,255,255,0.35);
    }
    html { scroll-behavior: auto; }
    body {
      font-family: 'Pretendard Variable','Pretendard',-apple-system,BlinkMacSystemFont,system-ui,sans-serif;
      background: #FFFFFF;
      color: #e8f0f8;
      overflow-x: hidden;
    }
    .font-display { font-family: 'Cormorant Garamond', Georgia, serif; }
    .font-impact { font-family: 'Black Han Sans', 'Pretendard Variable', sans-serif; }
    .font-bebas { font-family: 'Bebas Neue', 'Cormorant Garamond', serif; }
    .blue-gradient {
      background: linear-gradient(90deg, var(--blue), var(--blue-light), var(--blue));
      -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;
    }
    .section-label {
      display: block; font-size: 10px; letter-spacing: 0.5em; text-transform: uppercase;
      color: var(--blue); margin-bottom: 16px;
    }
    .section-divider {
      width: 40px; height: 1px;
      background: linear-gradient(90deg, var(--blue), var(--blue-light));
      margin: 20px auto;
    }
    .btn-primary {
      display: inline-block;
      background: linear-gradient(135deg, var(--blue-deep), #0D47A1);
      color: #FFFFFF; padding: 16px 40px;
      font-size: 11px; letter-spacing: 0.25em; text-transform: uppercase;
      text-decoration: none; transition: all 0.3s ease; border: none; cursor: pointer;
      font-family: inherit; position: relative; overflow: hidden;
    }
    .btn-primary::after {
      content:''; position:absolute; inset:0;
      background: linear-gradient(135deg,rgba(255,255,255,0.15),transparent);
      opacity:0; transition: opacity 0.3s;
    }
    .btn-primary:hover::after { opacity:1; }
    .btn-primary:hover { box-shadow: 0 16px 40px rgba(21,101,192,0.25); }
    .btn-outline {
      display: inline-block; border: 1px solid rgba(10,22,40,0.35);
      color: rgba(10,22,40,0.8); padding: 16px 40px;
      font-size: 11px; letter-spacing: 0.25em; text-transform: uppercase;
      text-decoration: none; transition: all 0.3s;
    }
    .btn-outline:hover { border-color: #1565C0; color: #1565C0; background: rgba(21,101,192,0.06); }
    .btn-blue {
      display: inline-block; border: 1px solid rgba(21,101,192,0.5); color: var(--blue);
      padding: 16px 40px; font-size: 11px; letter-spacing: 0.25em; text-transform: uppercase;
      text-decoration: none; transition: all 0.3s; background: transparent;
    }
    .btn-blue:hover { background: rgba(21,101,192,0.08); border-color: var(--blue); }

    /* ── 마키 ── */
    .marquee-outer { overflow: hidden; background: #F0F5FC; border-top: 1px solid rgba(21,101,192,0.1); border-bottom: 1px solid rgba(10,22,40,0.1); }
    .marquee-track { display: flex; white-space: nowrap; animation: marqueeAnim 28s linear infinite; }
    .marquee-item { font-family: 'Cormorant Garamond',serif; font-size: 13px; font-style: italic; letter-spacing: 0.3em; color: rgba(21,101,192,0.7); padding: 13px 36px; flex-shrink: 0; }
    @keyframes marqueeAnim { from { transform: translateX(0); } to { transform: translateX(-50%); } }

    /* ── 네비 ── */
    nav {
      position: fixed; top: 0; left: 0; right: 0; z-index: 100;
      padding: 20px 8vw;
      display: flex; align-items: center; justify-content: space-between;
      transition: background 0.5s ease, padding 0.3s ease;
    }
    nav.scrolled {
      background: rgba(255,255,255,0.96);
      backdrop-filter: blur(12px);
      -webkit-backdrop-filter: blur(12px);
      padding: 14px 8vw;
      border-bottom: 1px solid rgba(10,22,40,0.1);
    }
    .nav-logo {
      display: flex; align-items: center; gap: 10px;
      text-decoration: none; color: #0A1628;
    }
    .nav-logo img { height: 40px; width: auto; object-fit: contain; filter: drop-shadow(0 0 8px rgba(21,101,192,0.2)); }
    .nav-logo span { font-size: 15px; font-weight: 600; letter-spacing: 0.05em; color: #0A1628; }
    .nav-links { display: flex; gap: 36px; list-style: none; }
    .nav-links a {
      text-decoration: none; font-size: 11px; letter-spacing: 0.2em; text-transform: uppercase;
      color: rgba(10,22,40,0.7); transition: color 0.3s;
    }
    .nav-links a:hover, .nav-links a.active { color: var(--blue); }
    .nav-cta {
      font-size: 11px; letter-spacing: 0.2em; text-transform: uppercase;
      text-decoration: none; color: var(--blue);
      border: 1px solid rgba(10,22,40,0.3); padding: 9px 22px;
      transition: all 0.3s;
    }
    .nav-cta:hover { background: rgba(21,101,192,0.08); border-color: var(--blue); }
    .nav-hamburger {
      display: none; flex-direction: column; gap: 5px; cursor: pointer; background: none; border: none; padding: 4px;
    }
    .nav-hamburger span { display: block; width: 24px; height: 1.5px; background: #0A1628; transition: all 0.3s; }

    /* ── 모바일 메뉴 ── */
    .nav-mobile {
      display: none; position: fixed; inset: 0; z-index: 99;
      background: rgba(255,255,255,0.98); backdrop-filter: blur(20px);
      flex-direction: column; align-items: center; justify-content: center; gap: 36px;
    }
    .nav-mobile.open { display: flex; }
    .nav-mobile a {
      font-size: 20px; letter-spacing: 0.15em; text-transform: uppercase;
      text-decoration: none; color: rgba(10,22,40,0.85); transition: color 0.2s;
    }
    .nav-mobile a:hover { color: var(--blue); }

    /* ── 갤러리 그리드 ── */
    .hgal-item { position: relative; aspect-ratio: 4/3; overflow: hidden; }
    .hgal-item img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.6s ease; display: block; }
    .hgal-item:hover img { transform: scale(1.06); }
    .hgal-label { position: absolute; bottom: 0; left: 0; right: 0; padding: 12px 14px; background: linear-gradient(to top, rgba(10,22,40,0.75), transparent); color: rgba(255,255,255,0.9); font-size: 9px; letter-spacing: 0.25em; text-transform: uppercase; transform: translateY(4px); opacity: 0; transition: all 0.3s; }
    .hgal-item:hover .hgal-label { opacity: 1; transform: translateY(0); }
    @media (max-width: 768px) {
      .hgal-item { aspect-ratio: 1/1; }
    }

    /* ── Stats ── */
    .stat-item { text-align: center; padding: 40px 20px; position: relative; }
    .stat-item + .stat-item::before {
      content:''; position:absolute; left:0; top:50%; transform:translateY(-50%);
      width:1px; height:40px; background:rgba(21,101,192,0.15);
    }
    .stat-num {
      font-family: 'Cormorant Garamond',serif;
      font-size: clamp(52px, 8vw, 80px);
      font-weight: 300;
      background: linear-gradient(135deg, var(--blue), var(--blue-light));
      -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;
      line-height: 1; display: block;
    }
    .stat-label {
      font-size: 10px; letter-spacing: 0.4em; text-transform: uppercase;
      color: rgba(10,22,40,0.5); margin-top: 10px; display: block;
    }

    /* ── 리뷰 카드 ── */
    .review-card {
      background: #FFFFFF;
      border: 1px solid rgba(10,22,40,0.1);
      padding: 36px;
      box-shadow: 0 2px 12px rgba(10,22,40,0.08);
      transition: border-color 0.4s, background 0.4s, box-shadow 0.4s;
      opacity: 0; transform: translateY(40px);
    }
    .review-card:hover {
      border-color: rgba(21,101,192,0.3);
      background: #FFFFFF;
      box-shadow: 0 4px 20px rgba(10,22,40,0.12);
    }

    /* ── GSAP 애니메이션 초기 상태 ── */
    .gsap-fade { opacity: 0; transform: translateY(30px); }
    .gsap-fade-left { opacity: 0; transform: translateX(-30px); }
    .clip-reveal { clip-path: inset(0 100% 0 0); }
    .char { display: inline-block; }
    .magnetic { transition: transform 0.3s cubic-bezier(0.23, 1, 0.32, 1), box-shadow 0.3s; }

    /* ── 히어로 타이틀 애니메이션 ── */
    .title-slide-up {
      transform: translateY(110%);
      animation: titleSlideUp 1s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    }
    @keyframes titleSlideUp { to { transform: translateY(0); } }

    /* ── 스크롤 유도 바운스 ── */
    @keyframes bounce {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(6px); }
    }

    /* ── 푸터 ── */
    footer {
      background: #0A1628;
      border-top: 1px solid rgba(10,22,40,0.1);
      padding: 60px 8vw 40px;
    }
    footer a { color: rgba(10,22,40,0.65); text-decoration: none; transition: color 0.3s; }
    footer a:hover { color: var(--blue); }
    .footer-inner {
      max-width: 1100px; margin: 0 auto;
      display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 40px; margin-bottom: 50px;
    }
    .footer-logo { font-family: 'Cormorant Garamond',serif; font-size: 24px; font-weight: 300; color: #FFFFFF; margin-bottom: 12px; }
    .footer-desc { font-size: 13px; color: rgba(255,255,255,0.4); line-height: 1.8; }
    .footer-heading { font-size: 10px; letter-spacing: 0.4em; text-transform: uppercase; color: var(--blue); margin-bottom: 16px; }
    .footer-links { list-style: none; display: flex; flex-direction: column; gap: 10px; }
    .footer-links a { font-size: 13px; }
    .footer-bottom {
      max-width: 1100px; margin: 0 auto;
      padding-top: 24px; border-top: 1px solid rgba(255,255,255,0.06);
      display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 12px;
    }
    .footer-copy { font-size: 11px; color: rgba(255,255,255,0.35); }

    /* ── 플로팅 CTA ── */
    .float-cta {
      position: fixed; bottom: 28px; right: 24px; z-index: 200;
      display: flex; flex-direction: column; align-items: flex-end; gap: 10px;
    }
    .float-btn {
      width: 52px; height: 52px; border-radius: 50%;
      display: flex; align-items: center; justify-content: center;
      text-decoration: none; transition: all 0.3s;
      box-shadow: 0 8px 24px rgba(0,0,0,0.4);
    }
    .float-tel { background: linear-gradient(135deg, var(--blue-deep), #0D47A1); }
    .float-tel:hover { transform: scale(1.1); box-shadow: 0 12px 32px rgba(21,101,192,0.4); }
    .float-blog { background: #03C75A; }
    .float-blog:hover { transform: scale(1.1); }

    /* ── 모바일 bg show ── */
    @media (max-width: 900px) {
      .mobile-bg { display: block !important; }
    }

    /* ── 반응형 ── */
    @media (max-width: 768px) {
      .nav-links, .nav-cta { display: none; }
      .nav-hamburger { display: flex; }
      .footer-inner { grid-template-columns: 1fr; gap: 32px; }
      .stat-item + .stat-item::before { display: none; }
      .stat-item { padding: 28px 16px; border-bottom: 1px solid rgba(10,22,40,0.08); }
    }
  </style>
</head>
<body>
  <!-- NAV -->
  <nav id="main-nav">
    <a href={`${base}/`} class="nav-logo">
      <img src={`${base}/images/logo.png`} alt="DD에어컨 로고" />
      <span>디디에어컨</span>
    </a>
    <ul class="nav-links">
      <li><a href={`${base}/`}>홈</a></li>
      <li><a href={`${base}/cases`}>시공사례</a></li>
      <li><a href={`${base}/pricing`}>설치비용</a></li>
      <li><a href={`${base}/info`}>에어컨정보</a></li>
      <li><a href={`${base}/subsidy`} style="color:var(--blue);font-weight:600;">정부지원사업</a></li>
      <li><a href={`${base}/contact`}>상담신청</a></li>
    </ul>
    <a href="tel:010-7237-2122" class="nav-cta">010-7237-2122</a>
    <button class="nav-hamburger" id="hamburger" aria-label="메뉴 열기">
      <span></span><span></span><span></span>
    </button>
  </nav>

  <!-- 모바일 메뉴 -->
  <div class="nav-mobile" id="mobile-nav">
    <a href={`${base}/`} onclick="closeMobileNav()">홈</a>
    <a href={`${base}/cases`} onclick="closeMobileNav()">시공사례</a>
    <a href={`${base}/pricing`} onclick="closeMobileNav()">설치비용</a>
    <a href={`${base}/info`} onclick="closeMobileNav()">에어컨정보</a>
    <a href={`${base}/subsidy`} onclick="closeMobileNav()" style="color:var(--blue);">정부지원사업</a>
    <a href={`${base}/contact`} onclick="closeMobileNav()">상담신청</a>
    <a href="tel:010-7237-2122" style="color:var(--blue);font-size:22px;letter-spacing:0.1em;">010-7237-2122</a>
  </div>

  <slot />

  <!-- 플로팅 CTA -->
  <div class="float-cta">
    <a href="https://open.kakao.com/o/ddinair" target="_blank" rel="noopener" class="float-btn" style="background:#FEE500;" title="카카오톡 문의">
      <svg width="22" height="22" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
        <path d="M12 3C7.03 3 3 6.36 3 10.5c0 2.7 1.67 5.07 4.2 6.53l-.88 3.28c-.08.29.22.52.48.36L11.07 18c.3.03.61.04.93.04 4.97 0 9-3.36 9-7.54C21 6.36 16.97 3 12 3z" fill="#3B1E1E"/>
      </svg>
    </a>
    <a href="https://blog.naver.com/ddinair" target="_blank" rel="noopener" class="float-btn float-blog" title="네이버 블로그">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="white">
        <path d="M16.273 12.845L7.376 0H0v24h7.727V11.155L16.624 24H24V0h-7.727z"/>
      </svg>
    </a>
    <a href="tel:010-7237-2122" class="float-btn float-tel" title="전화 문의">
      <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2">
        <path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07A19.5 19.5 0 013.83 10.8 19.79 19.79 0 01.77 2.18 2 2 0 012.77 0h3a2 2 0 012 1.72c.127.96.361 1.903.7 2.81a2 2 0 01-.45 2.11L6.91 7.78a16 16 0 006.29 6.29l1.15-1.15a2 2 0 012.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0122 16.92z"/>
      </svg>
    </a>
  </div>

  <footer>
    <div class="footer-inner">
      <div>
        <div class="footer-logo">디디에어컨</div>
        <p class="footer-desc">남양주·다산신도시 에어컨 전문<br/>설치·판매·수리·이전</p>
        <p style="font-size:12px;color:rgba(255,255,255,0.3);margin-top:16px;line-height:1.8;">
          경기도 남양주시 진건읍 고재로 186 1층<br/>
          TEL: 010-7237-2122
        </p>
      </div>
      <div>
        <div class="footer-heading">서비스</div>
        <ul class="footer-links">
          <li><a href={`${base}/`}>시스템 에어컨 설치</a></li>
          <li><a href={`${base}/`}>에어컨 수리·이전</a></li>
          <li><a href={`${base}/`}>에어컨 이전설치</a></li>
          <li><a href={`${base}/cases`}>시공사례</a></li>
        </ul>
      </div>
      <div>
        <div class="footer-heading">바로가기</div>
        <ul class="footer-links">
          <li><a href="https://blog.naver.com/ddinair" target="_blank" rel="noopener">네이버 블로그</a></li>
          <li><a href="https://map.naver.com/v5/search/DD에어컨" target="_blank" rel="noopener">네이버 플레이스</a></li>
          <li><a href={`${base}/contact`}>서비스 신청</a></li>
        </ul>
        <div style="margin-top:20px;">
          <p style="font-size:10px;letter-spacing:0.3em;color:rgba(21,101,192,0.8);text-transform:uppercase;margin-bottom:8px;">서비스 권역</p>
          <p style="font-size:12px;color:rgba(255,255,255,0.4);line-height:1.8;">
            남양주 · 다산신도시 · 구리 · 하남<br/>의정부 · 노원 · 서울 전역
          </p>
        </div>
        <div style="margin-top:24px;display:flex;gap:10px;align-items:center;">
          <p style="font-size:10px;letter-spacing:0.3em;color:rgba(21,101,192,0.8);text-transform:uppercase;">소셜</p>
          <a href="https://blog.naver.com/ddinair" target="_blank" rel="noopener"
             style="display:flex;align-items:center;justify-content:center;width:36px;height:36px;border-radius:6px;background:#03C75A;" title="네이버 블로그">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="white">
              <path d="M16.273 12.845L7.376 0H0v24h7.727V11.155L16.624 24H24V0h-7.727z"/>
            </svg>
          </a>
          <a href="tel:010-7237-2122"
             style="display:flex;align-items:center;justify-content:center;width:36px;height:36px;border-radius:6px;background:#FEE500;" title="카카오톡 문의">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="#3C1E1E">
              <path d="M12 2C6.477 2 2 5.85 2 10.6c0 2.956 1.768 5.558 4.444 7.18-.165.615-.597 2.23-.684 2.576-.108.432.158.426.332.31.136-.092 2.168-1.47 3.046-2.065.609.087 1.232.133 1.862.133 5.523 0 10-3.85 10-8.6C22 5.85 17.523 2 12 2z"/>
            </svg>
          </a>
        </div>
      </div>
    </div>
    <div class="footer-bottom">
      <p class="footer-copy">© 2024 DD에어컨. All rights reserved.</p>
      <p class="footer-copy">남양주 에어컨 전문 설치·청소·수리</p>
    </div>
  </footer>

  <script is:inline>
    // Lenis smooth scroll (기본 raf — GSAP 페이지는 load 후 재연결)
    window.addEventListener('load', function() {
      var LenisClass = window.Lenis;
      if (!LenisClass || window.__lenis) return;
      var lenis = new LenisClass({ lerp: 0.08, smoothWheel: true });
      window.__lenis = lenis;
      function raf(time) { lenis.raf(time); requestAnimationFrame(raf); }
      requestAnimationFrame(raf);
    });

    // Nav 스크롤
    const nav = document.getElementById('main-nav');
    window.addEventListener('scroll', () => nav?.classList.toggle('scrolled', window.scrollY > 60), { passive: true });

    // 햄버거 메뉴
    const hamburger = document.getElementById('hamburger');
    const mobileNav = document.getElementById('mobile-nav');
    hamburger?.addEventListener('click', () => {
      mobileNav.classList.toggle('open');
      document.body.style.overflow = mobileNav.classList.contains('open') ? 'hidden' : '';
    });
    function closeMobileNav() {
      mobileNav.classList.remove('open');
      document.body.style.overflow = '';
    }
    window.closeMobileNav = closeMobileNav;

    // Active nav link
    const currentPath = window.location.pathname;
    document.querySelectorAll('.nav-links a').forEach(a => {
      const href = a.getAttribute('href') || '';
      if (href === currentPath || (href !== '/' && currentPath.includes(href.split('/').pop() || ''))) {
        a.classList.add('active');
      }
    });
  </script>
</body>
</html>
